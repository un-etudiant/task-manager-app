import { type DataConfig } from '@aws-amplify/core';

export const dataConfig: DataConfig = {
  region: 'us-east-1', // Replace with your region
  apiUrl: 'your-appsync-api-url', // Replace with your AppSync API URL
  apiKey: 'your-appsync-api-key', // Replace with your AppSync API Key
};